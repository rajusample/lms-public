 pipeline {
    agent any

    stages {
        stage('sonar analysis') {
            steps {
                sh 'cd webapp && docker run --rm -e SONAR_HOST_URL=http://54.177.82.147:9000 -v .:/usr/src -e SONAR_TOKEN=sqa_bd5274b6218fe83dbdef10d15c330dd107da09a sonarsource/sonar-scanner-cli -Dsonar.projectKey=lms
                echo 'CODE QUALITY COMPLETED'   
            }
        }
    }
}
